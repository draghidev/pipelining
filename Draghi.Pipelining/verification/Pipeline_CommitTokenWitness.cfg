\* CONVICTION WITNESS - EXPECTED VIOLATION of NoStaleTokenRead (July 2026, consumable-token round).
\*
\* Formalizes the pump-fault convicted UPDATE 8 (2026-07-04): CommitWaiter reads a SINGLE-CONSUMPTION
\* ValueTask AFTER a concurrent drain has GetResult'd (Consumed) it, observing a recycled promise box
\* and throwing (Slon MRVTSC.GetStatus token-validation). The shipped commit protocol
\* (CommitOwnershipRestructure = FALSE) keeps four post-publication task reads, ALL the same illegal
\* class - Pipeline.cs:
\*   1136  wasEmpty && !waiterTask.IsCompleted        (out-of-lock, modeled ExecCommitQueueCountArm)
\*   1152  if (!_liveActivation && !waiterTask.IsCompleted)  (in-lock recheck, ExecCommitQueueRecheck)
\*   1169  if (waiterTask.IsCompleted && ...)          (verify done-leg, ExecCommitQueueVerify)
\*   1185  if (waiterTask.IsCompleted)                 (callback-wiring - THE CONVICTED THROWER,
\*                                                      modeled at the commit terminals)
\* The trace shape: the committer publishes item i to the escalated queue (VisibleWins), i completes
\* (CompleteTask), a concurrent advancer drains + GetResults it (AdvancerDrainHead = ConsumeToken),
\* then the committer - still parked mid-self-act for i - performs its next post-publication task read
\* and hits the Consumed token (staleTokenRead set). Full split-commit fidelity (SplitCommitSelfActivate
\* /\ SplitCommitSelfActRecheck /\ CommitSelfActVerify) so the recheck and verify reads are present.
\* EXPECTED: NoStaleTokenRead VIOLATED. The fix is Pipeline_CommitOwnershipFix.cfg (green).
CONSTANTS
  NumItems = 3
  WeakAdvancerRelease = FALSE
  WeakHasExecutingPublish = TRUE
  IsReferenceT = FALSE
  EmptySignalDeferred = TRUE
  NudgeEnabled = TRUE
  SlotReclaimEnabled = TRUE
  SlotChainActivation = TRUE
  SplitCountCommit = TRUE
  SkewTolerantPartition = TRUE
  PassOnceDrain = TRUE
  SignalConservation = TRUE
  CountGatedConservation = TRUE
  PendingWordLatch = TRUE
  SplitSlotFieldOps = TRUE
  TriStateSlotClaim = TRUE
  ModelCPathClear = TRUE
  CPathLeaveFix = TRUE
  SplitCallbackOps = FALSE
  SplitCPathExchange = TRUE
  SingleActivationGate = TRUE
  PeekGatedCPathLicense = TRUE
  QueueDPathCompletedGuard = TRUE
  LockFreeIdleDispatch = TRUE
  SplitIdleDispatchGuard = FALSE
  RecoverySplit = FALSE
  CompleteBeforeCount = TRUE
  ConsumableTaskTokens = TRUE
  CommitOwnershipRestructure = FALSE
  RestructureWireBeforePublish = FALSE
  CPathPendingPreCheck = TRUE
  SplitCPathPreCheck = FALSE
  SplitCommitSelfActivate = TRUE
  CommitSelfActRecheck = TRUE
  SplitCommitSelfActRecheck = TRUE
  CommitSelfActVerify = TRUE
  CommitSelfActVerifyOnTaken = FALSE
  SplitRecoveryInlineAct = FALSE
  QueueTrailingFault = TRUE
SPECIFICATION Spec
INVARIANT NoStaleTokenRead
CHECK_DEADLOCK FALSE

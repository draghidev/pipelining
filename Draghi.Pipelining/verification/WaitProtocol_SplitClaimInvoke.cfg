\* Stolen-thread dispatch fidelity (July 2026 strand hunt): the async claim's invoke is a
\* QUEUED IThreadPoolWorkItem that runs an unbounded time later, not part of the claim step.
\* All fixes ON; SplitClaimInvoke opens the claimed-but-not-yet-resumed window to every other
\* actor. Expectation: everything HOLDS - the protocol should be dispatch-delay-tolerant
\* (claims are pending-gated exactly-once and arms only follow resumes). A violation here is
\* a candidate mechanism for the observed FIFO-violating permanent strand
\* (NoTwoItemsActivatedConcurrently iter 203/602).
SPECIFICATION Spec
CONSTANTS
  MaxItems = 3
  RecheckFix = TRUE
  LockThroughFix = TRUE
  ClearOnClaimFix = TRUE
  SplitClaimInvoke = TRUE
INVARIANT TypeOK
INVARIANT NoNullInvoke
INVARIANT HandoffInline
PROPERTY AllConsumed
PROPERTY HandoffReturns

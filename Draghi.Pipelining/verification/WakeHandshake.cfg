\* Both StoreLoad edges fenced (Interlocked.Exchange both sides). This is the SHARED
\* fix state of BOTH features: D5 drain-wakeup (arm fenced + the already-fenced release)
\* and #5 shutdown handoff (both writes Exchange). EXPECTED: NoLostWake HOLDS.
SPECIFICATION Spec
CONSTANTS
  FenceA = TRUE
  FenceB = TRUE
INVARIANT TypeOK
PROPERTY NoLostWake
CHECK_DEADLOCK FALSE

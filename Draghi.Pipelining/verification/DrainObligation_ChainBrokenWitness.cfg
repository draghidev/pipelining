\* WITNESS for the captured strand (DrainTrace tape, July 2026): consumer
\* cached view with the happens-before chain BROKEN - the acquire read of the
\* segment's _last (and the plain _next read) may return any value the
\* location held at/after the thread's own last read of it (adversarial
\* staleness, coherence only; no edge harvesting). VIOLATION (2026-07-02),
\* 27-state CE: e1 in A, e2 (completed) + e3 in B; e2's own pass misses on a
\* stale-empty A view (broken s1 = lastCopy, stale-null _next), conserve
\* restores, the deposit-served second pass drains e1 but its stale-null
\* _next read never follows into B - drainedAny skips conserve, the recheck
\* reads a consumed token, break. e2 completed + resident forever, every
\* callback spent: the permanent strand. The tape's exact stale-equal-_last +
\* reclaim-miss-break face is also in this config's behavior set (it is the
\* CachedViewSound counterexample's schedule, which needs no harvest). Pins
\* that adversarial staleness + the pass/reclaim miss exits = the capture:
\* the strand is exactly one broken (or legally stale) read away.
SPECIFICATION Spec
CONSTANTS
    NumEntries = 3
    ConserveRestoreEnabled = TRUE
    ConsumerCachedView = TRUE
    ChainSound = FALSE
    ReclaimMissRetry = FALSE
    FreshHopGuard = FALSE
    SegCapacity = 2
INVARIANTS
    TypeOK
    ViewCoherent
    LatchOwnership
    CountSkewFloor
    RetiredNotResident
    RetiredImpliesCompleted
PROPERTIES
    EventuallyDrained

\* WITNESS (expect RED / deadlock): the producer fence between the enqueue store
\* and the pumpWord read WEAKENED to release-only (FastPathFence=FALSE) under TSO.
\* The item sits in the producer store buffer while it reads Running and returns;
\* the pump goes Idle and rechecks an EMPTY visible prefix; the item commits later
\* with no token and no redrive. Proves the full fence is load-bearing.
SPECIFICATION Spec
CONSTANTS
  NSync = 0
  NAsync = 2
  G1 = TRUE
  G2 = TRUE
  G4 = TRUE
  G5 = TRUE
  G6 = TRUE
  G7 = TRUE
  G8 = TRUE
  LockThrough = TRUE
  PublishFence = FALSE
  AllowTrailing = TRUE
  FastPath = TRUE
  FastPathFence = FALSE
  IdleAtRelinquish = FALSE
INVARIANTS
  TypeOK
  SingleRunner
  OneDriver
  NoNullInvoke
  NoDoubleProc
  FifoSync
PROPERTIES
  AllProcessed
  SyncReturns
CHECK_DEADLOCK TRUE

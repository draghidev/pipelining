\* Witness probe: signal-conservation restore disabled. GREEN (2026-07-02):
\* EventuallyDrained still holds, so the restore is NOT load-bearing in
\* steady-state queue mode - its real weight sits at the escalation-nudge seam
\* (Pipeline.cs 1153-1159, cut from this module), the one consumer that reads
\* the token to DECIDE whether to acquire. Truthful queue view (base model).
SPECIFICATION Spec
CONSTANTS
    NumEntries = 3
    ConserveRestoreEnabled = FALSE
    ConsumerCachedView = FALSE
    ChainSound = TRUE
    ReclaimMissRetry = FALSE
    FreshHopGuard = FALSE
    SegCapacity = 2
INVARIANTS
    TypeOK
    ViewCoherent
    LatchOwnership
    CountSkewFloor
    RetiredNotResident
    RetiredImpliesCompleted
PROPERTIES
    EventuallyDrained

\* FIX PROBE against the broken-chain witness: ChainSound = FALSE plus
\* ReclaimMissRetry. The retry is modeled WITH full-fence freshness (its
\* reads return true last; a weaker fence-only retry can re-miss against
\* unbounded staleness - see module header). VERDICT (2026-07-02): VIOLATION,
\* 27-state CE - the retry does NOT close the witness. The strand routes
\* around the reclaim entirely: the deposit-served pass drains e1, its
\* stale-null _next read never follows into published B (e2 completed,
\* resident), drainedAny skips conserve, and the recheck breaks on the
\* consumed token - the retry site is never reached. Even a truthfully fresh
\* reclaim re-peek cannot rescue a strand whose exit is the drainedAny
\* recheck-FALSE break or a committed wrong hop.
SPECIFICATION Spec
CONSTANTS
    NumEntries = 3
    ConserveRestoreEnabled = TRUE
    ConsumerCachedView = TRUE
    ChainSound = FALSE
    ReclaimMissRetry = TRUE
    FreshHopGuard = FALSE
    SegCapacity = 2
INVARIANTS
    TypeOK
    ViewCoherent
    LatchOwnership
    CountSkewFloor
    RetiredNotResident
    RetiredImpliesCompleted
PROPERTIES
    EventuallyDrained

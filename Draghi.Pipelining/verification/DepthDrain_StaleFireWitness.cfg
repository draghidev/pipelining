\* Witness: the blind deferred fire (audit C4, the shipped shape). The zero
\* verdict is bump-instant and OnDepthReachedZero is deferred past the advancer
\* release; a producer + a new WaitForEmptyAsync arm land in the window and the
\* stale zero takes the NEW tcs - the caller wakes on evidence predating its
\* arm (NoPrematureWake violated).
SPECIFICATION Spec
CONSTANTS
  MaxEnq = 4
  Completers = {c1, c2}
  RecheckFix = TRUE
  RevalidateFix = FALSE
INVARIANT TypeOK
INVARIANT NoPrematureWake
CHECK_DEADLOCK FALSE

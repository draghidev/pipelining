\* Queue-mode drain obligation transfer, full-split fidelity (shipped shape).
\* ConsumerCachedView = FALSE: the queue is a truthful atomic FIFO (base model;
\* ChainSound/ReclaimMissRetry are inert here). Deadlock detection ON (default):
\* any stuck state outside the Done self-loop is a strand on tape.
SPECIFICATION Spec
CONSTANTS
    NumEntries = 3
    ConserveRestoreEnabled = TRUE
    ConsumerCachedView = FALSE
    ChainSound = TRUE
    ReclaimMissRetry = FALSE
    FreshHopGuard = FALSE
    SegCapacity = 2
INVARIANTS
    TypeOK
    ViewCoherent
    LatchOwnership
    CountSkewFloor
    RetiredNotResident
    RetiredImpliesCompleted
PROPERTIES
    EventuallyDrained

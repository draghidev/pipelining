\* D5 drain-wakeup, pre-fix: the waiter's arm is release-only (Volatile.Write) while
\* the callback/release side is already an Interlocked RMW. Config = (FenceA=FALSE,
\* FenceB=TRUE). EXPECTED: NoLostWake VIOLATED - the waiter parks, the signaler
\* skips, lost wake (the original DrainWakeup_LostWakeWitness, now a config here).
SPECIFICATION Spec
CONSTANTS
  FenceA = FALSE
  FenceB = TRUE
INVARIANT TypeOK
PROPERTY NoLostWake
CHECK_DEADLOCK FALSE

\* Witness: the shipped flag-only wait re-check (audit C6). The producer's
\* flag store lands after the consumer's dequeue-to-empty clear: flag TRUE,
\* queue empty, and the re-check retries forever - the executor spins instead
\* of arming and completed-resolution is starved (AllConsumed violated).
SPECIFICATION Spec
CONSTANTS
  MaxItems = 2
  CloseFence = TRUE
  SignalFence = TRUE
  GateUnderLock = TRUE
  PeekRecheck = FALSE
INVARIANTS
  TypeOK
  NoWrongThreadTake
  HandoffInline
PROPERTIES
  Drains
  AllConsumed
  HandoffReturns
CHECK_DEADLOCK FALSE

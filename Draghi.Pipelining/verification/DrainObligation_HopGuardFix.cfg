\* THE FIX, red-before-green closure: identical to
\* DrainObligation_CachedViewSound.cfg (faithful cached view, SOUND
\* happens-before chain) plus FreshHopGuard = TRUE - the hop guard's
\* emptiness side re-reads TRUE last at hop time (the shipped Volatile.Read
\* in TryDequeueSlow / TryDequeueIf's slow-path hop condition), raising the
\* reader's floor. Hop-on-fresh-equal is stable (_next published freezes the
\* segment's _last). Expected GREEN: EventuallyDrained holds, deadlock
\* detection on - the wrong-hop finding closed at its source.
SPECIFICATION Spec
CONSTANTS
    NumEntries = 3
    ConserveRestoreEnabled = TRUE
    ConsumerCachedView = TRUE
    ChainSound = TRUE
    ReclaimMissRetry = FALSE
    FreshHopGuard = TRUE
    SegCapacity = 2
INVARIANTS
    TypeOK
    ViewCoherent
    LatchOwnership
    CountSkewFloor
    RetiredNotResident
    RetiredImpliesCompleted
PROPERTIES
    EventuallyDrained

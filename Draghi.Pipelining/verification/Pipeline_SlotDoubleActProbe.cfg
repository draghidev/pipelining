\* PROBE (July 2026, gate round follow-up): does the SLOT C-path have the same face as the
\* queue double-act finding? SlotDrainCompleteCPath is also count-licensed (its under-lock
\* re-read is `storeCount <= 0`, not a residency peek) and is deliberately NOT gated by
\* SingleActivationGate (the code's DrainSlotInline C-path carries no _liveActivation
\* check). The queue witness's chain was: completed-before-commit head resident-but-
\* uncounted -> count-0 license fires the deferred publish out of FIFO order -> the
\* completed head's retirement clear wrongly releases the live turn -> ungated D-arm
\* re-activates the same item. The slot-tier analogue of resident-but-uncounted is the
\* SplitSlotFieldOps commit window (fields landed, increment pending).
\*
\* Constants: the DoubleAct witness's slot-arm tree (TriState claim + split slot field ops,
\* as Pipeline_Contract) with the QUEUE face structurally closed so any violation is
\* attributable to slot mode alone: SplitCPathExchange = FALSE (the fused queue C-path
\* reads residency atomically - no stale-arm window) and both fix toggles FALSE (probing
\* the SHIPPED slot shape, not the fix). Shipped gate ON, as in the witness.
\*
\* VERDICT (2026-07-02, TLC -workers 4): ActivatedAtMostOnce HOLDS - 2,655,385 states
\* generated, 861,951 distinct, depth 51, no error. No slot-mode cousin at this fidelity:
\* the slot C-path's count-then-consume runs as ONE fused lock-held action
\* (SlotDrainCompleteCPath models that linearization - a commit's publish Exchange
\* precedes its increment, so whichever Exchange wins owns the activation exactly once),
\* and the slot chain/handoff D-arms already carry the IsCompleted skip. A finer split of
\* the slot C-path's count read from its publish Exchange (the SplitCPathExchange
\* treatment applied to DrainSlotInline) is the remaining fidelity gap if this face is
\* ever suspected in slot mode.
CONSTANTS
  NumItems = 3
  WeakAdvancerRelease = FALSE
  WeakHasExecutingPublish = TRUE
  IsReferenceT = FALSE
  EmptySignalDeferred = TRUE
  NudgeEnabled = TRUE
  SlotReclaimEnabled = TRUE
  SlotChainActivation = TRUE
  SplitCountCommit = TRUE
  SkewTolerantPartition = TRUE
  PassOnceDrain = TRUE
  SignalConservation = TRUE
  CountGatedConservation = TRUE
  PendingWordLatch = TRUE
  SplitSlotFieldOps = TRUE
  TriStateSlotClaim = TRUE
  ModelCPathClear = TRUE
  CPathLeaveFix = TRUE
  SplitCallbackOps = FALSE
  SplitCPathExchange = FALSE  \* queue C-path fused: the queue face is structurally closed
  SingleActivationGate = TRUE  \* shipped gate ON, as in the witness
  PeekGatedCPathLicense = FALSE  \* shipped shape - the probe targets the slot path
  QueueDPathCompletedGuard = FALSE
  LockFreeIdleDispatch = FALSE
  SplitIdleDispatchGuard = FALSE
  CPathPendingPreCheck = FALSE
  SplitCPathPreCheck = FALSE
  SplitCommitSelfActivate = FALSE
  CommitSelfActRecheck = FALSE
  SplitCommitSelfActRecheck = FALSE
  CommitSelfActVerify = FALSE
  CommitSelfActVerifyOnTaken = FALSE
  SplitRecoveryInlineAct = FALSE
  QueueTrailingFault = TRUE
  RecoverySplit = FALSE
  CompleteBeforeCount = TRUE
  ConsumableTaskTokens = FALSE
  CommitOwnershipRestructure = FALSE
  RestructureWireBeforePublish = FALSE
SPECIFICATION Spec
INVARIANT ActivatedAtMostOnce
CHECK_DEADLOCK FALSE

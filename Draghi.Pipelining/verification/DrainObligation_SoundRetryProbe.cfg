\* FIX PROBE against the sound-chain violation: ChainSound = TRUE plus
\* ReclaimMissRetry - one fenced re-peek before the reclaim's final break.
\* The retry is modeled WITH full-fence freshness (its reads return true
\* last; floors raised to producer truth - see module header). VERDICT
\* (2026-07-02): DEADLOCK, 30-state CE - the retry does NOT close the
\* finding. The wrong segment hop is a committed _head advance, not a stale
\* read: with all three entries completed, the post-hop passes drain B,
\* verdict truthfully B-empty, and the A-resident entry strands with every
\* callback spent. The retry is never even reached: a pass that drained
\* something (drainedAny) skips signal conservation, so its exit breaks at
\* the recheck with the token consumed - no reclaim, no retry site.
SPECIFICATION Spec
CONSTANTS
    NumEntries = 3
    ConserveRestoreEnabled = TRUE
    ConsumerCachedView = TRUE
    ChainSound = TRUE
    ReclaimMissRetry = TRUE
    FreshHopGuard = FALSE
    SegCapacity = 2
INVARIANTS
    TypeOK
    ViewCoherent
    LatchOwnership
    CountSkewFloor
    RetiredNotResident
    RetiredImpliesCompleted
PROPERTIES
    EventuallyDrained

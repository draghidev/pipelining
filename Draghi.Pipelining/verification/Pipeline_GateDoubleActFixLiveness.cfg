\* FIX LIVENESS (July 2026, gate round): the double-act fix must not STRAND. Both fix
\* halves refuse work and leave an obligation behind:
\*   - a declined peek-gated license (resident entry found at fire time) LEAVES the
\*     pending publish, on the claim "the pass drains the resident head in FIFO order and
\*     the C-path re-fires once the store is empty by residency";
\*   - a guarded D-arm skip LEAVES the completed head resident, on the claim "the drain
\*     loop's own next iteration dequeues it".
\* Both are coverage claims of exactly the kind Pipeline_GateLiveness.cfg adjudicated for
\* the gate's two skips - so this run adjudicates them the same way: same constants as
\* Pipeline_GateLiveness.cfg (SplitCPathExchange = FALSE, the shipped gate ON) with BOTH
\* fix toggles TRUE, EventuallyCompleted + EventuallyActivated over the full fixed tree.
\*
\* EXPECTED GREEN. A liveness violation here means the fix strands an activation or a
\* completion - report the trace verbatim; do NOT weaken anything to force green.
CONSTANTS
  NumItems = 3
  WeakAdvancerRelease = FALSE
  WeakHasExecutingPublish = TRUE
  IsReferenceT = FALSE
  EmptySignalDeferred = TRUE
  NudgeEnabled = TRUE
  SlotReclaimEnabled = TRUE
  SlotChainActivation = TRUE
  SplitCountCommit = TRUE
  SkewTolerantPartition = TRUE
  PassOnceDrain = TRUE
  SignalConservation = TRUE
  CountGatedConservation = TRUE
  PendingWordLatch = TRUE
  SplitSlotFieldOps = TRUE
  TriStateSlotClaim = TRUE
  ModelCPathClear = TRUE
  CPathLeaveFix = TRUE
  SplitCallbackOps = FALSE
  SplitCPathExchange = FALSE
  SingleActivationGate = TRUE  \* the shipped gate, as in GateLiveness
  PeekGatedCPathLicense = TRUE
  QueueDPathCompletedGuard = TRUE
  LockFreeIdleDispatch = FALSE
  SplitIdleDispatchGuard = FALSE
  CPathPendingPreCheck = FALSE
  SplitCPathPreCheck = FALSE
  SplitCommitSelfActivate = FALSE
  CommitSelfActRecheck = FALSE
  SplitCommitSelfActRecheck = FALSE
  CommitSelfActVerify = FALSE
  CommitSelfActVerifyOnTaken = FALSE
  SplitRecoveryInlineAct = FALSE
  QueueTrailingFault = TRUE
  RecoverySplit = FALSE
  CompleteBeforeCount = TRUE
  ConsumableTaskTokens = FALSE
  CommitOwnershipRestructure = FALSE
  RestructureWireBeforePublish = FALSE
SPECIFICATION Spec
INVARIANT Invariants
PROPERTY EventuallyCompleted
PROPERTY EventuallyActivated
CHECK_DEADLOCK FALSE

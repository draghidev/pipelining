\* #5 shutdown caller<->executor handoff, pre-fix: BOTH sides Volatile.Write (the
\* standalone HangRepro, iter 51 on Apple Silicon - executor parked on a TCS the
\* caller already read null). Config = (FenceA=FALSE, FenceB=FALSE). EXPECTED:
\* NoLostWake VIOLATED. This is the config DrainWakeup's run never executes - which
\* is why #5 needed its own checked config, not coverage by pattern.
SPECIFICATION Spec
CONSTANTS
  FenceA = FALSE
  FenceB = FALSE
INVARIANT TypeOK
PROPERTY NoLostWake
CHECK_DEADLOCK FALSE

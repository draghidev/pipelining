\* ADJUDICATION: faithful SPSC consumer cached view + SOUND happens-before
\* chain (reads bounded below by every real synchronization edge: callback
\* wiring, latch-word RMW chain, count-word RMW chain, the Volatile.Read
\* count acquire, the _activationLock edge - see module header). Question:
\* can the captured permanent strand occur WITHOUT a broken acquire?
\* VIOLATION (2026-07-02), 30-state CE - THE FINDING: the WRONG SEGMENT HOP.
\* TryDequeueSlow's guard `_next != null && _first == currentLast`
\* (SingleProducerSingleConsumerQueue.cs 232) compares a fresh _next against
\* the CACHED LOCAL currentLast; with currentLast read while A was
\* (momentarily or apparently) empty and the producer's fill-A-then-publish-B
\* burst landing before the _next read, the consumer hops _head to B past
\* resident A entries - irreversibly. The CE then replays the tape verbatim:
\* serving pass fast-peeks B's uncompleted head, drains nothing, conserve
\* restores, reclaim re-peeks, re-misses, miss-break; the completed entry
\* stays resident in A forever with every callback spent. The interleaving is
\* legal even under sequential consistency (a preempted split read - no weak
\* memory required; the BCL original re-reads volatile _last at this guard).
\* All safety invariants held: the latch / deposit / conserve protocol is
\* exonerated - the queue's hop guard is the bug.
SPECIFICATION Spec
CONSTANTS
    NumEntries = 3
    ConserveRestoreEnabled = TRUE
    ConsumerCachedView = TRUE
    ChainSound = TRUE
    ReclaimMissRetry = FALSE
    FreshHopGuard = FALSE
    SegCapacity = 2
INVARIANTS
    TypeOK
    ViewCoherent
    LatchOwnership
    CountSkewFloor
    RetiredNotResident
    RetiredImpliesCompleted
PROPERTIES
    EventuallyDrained
